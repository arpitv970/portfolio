export interface ProjectType {
  id: number,
  title: string,
  img: string,
  bullets: string[],
  demo: string,
  code: string,
}

export interface NavElementType {
  pageTitle: string,
  url: string
}
